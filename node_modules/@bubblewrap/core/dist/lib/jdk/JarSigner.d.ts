import { JdkHelper } from './JdkHelper';
import { SigningKeyInfo } from '../../lib/TwaManifest';
/**
 * Wraps the Java `jarsigner` CLI tool.
 */
export declare class JarSigner {
    private jdkHelper;
    constructor(jdkHelper: JdkHelper);
    /**
     * Signs a file
     */
    sign(signingKeyInfo: SigningKeyInfo, storepass: string, keypass: string, inputFile: string, outputFile: string): Promise<void>;
}
