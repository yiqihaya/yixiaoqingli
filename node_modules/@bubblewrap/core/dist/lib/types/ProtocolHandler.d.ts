export interface ProtocolHandler {
    protocol: string;
    url: string;
}
export declare function processProtocolHandlers(protocolHandlers: ProtocolHandler[], startUrl: URL, scopeUrl: URL): ProtocolHandler[];
export declare function normalizeProtocolForTesting(protocol: string): string | undefined;
export declare function normalizeUrlForTesting(url: string, startUrl: URL, scopeUrl: URL): string | undefined;
