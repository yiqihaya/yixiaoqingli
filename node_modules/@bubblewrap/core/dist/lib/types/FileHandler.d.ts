export interface FileHandlerJson {
    action?: string;
    accept?: {
        [mimeType: string]: Array<string>;
    };
}
export interface FileHandler {
    actionUrl: string;
    mimeTypes: Array<string>;
}
export declare function processFileHandlers(fileHandlers: FileHandlerJson[], startUrl: URL, scopeUrl: URL): FileHandler[];
